name: Build

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: esp-idf build
      uses: sebastianPsm/esp-idf-ci-action@1